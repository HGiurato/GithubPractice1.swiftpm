import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            HStack{
                Rectangle()
                    .background(.purple)
                    .border(.yellow)
                    
            }
        }
    }
}
